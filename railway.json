{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node railway-prevent-nextjs.js && echo 'ERROR: Railway should NOT build from repository root. Backend must be in a separate directory.' && exit 1"
  },
  "deploy": {
    "startCommand": "node railway-prevent-nextjs.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

